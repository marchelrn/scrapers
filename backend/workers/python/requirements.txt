# Dependensi worker scraping BPS.
#
# Berkas ini hanya memuat paket yang benar-benar diimpor oleh worker, ditambah
# dependensi transitifnya yang perlu dipatok versinya. Sebelumnya berkas ini
# merupakan hasil `pip freeze` dan masih memuat pustaka milik scraper yang sudah
# dihapus (selenium, pyppeteer, requests-html, pyquery) serta fake-useragent yang
# tidak lagi dipakai sejak identitas User-Agent dikelola terpusat di fetcher.py.
#
# Instalasi browser Playwright dilakukan terpisah:  playwright install chromium

# --- Dependensi langsung ---------------------------------------------------
beautifulsoup4==4.15.0
lxml==6.1.1
playwright==1.62.0
requests==2.34.2

# Diperlukan hanya bila proxy SOCKS5 dikonfigurasi (lihat tingkat proxy pada
# docs/PANDUAN_METODE_SCRAPING_DAN_ANTI_BLOKIR.md). Tanpa paket ini, proxy
# skema socks5:// akan gagal dengan galat "Missing dependencies for SOCKS".
PySocks==1.7.1

# --- Dependensi transitif (dipatok agar build dapat direproduksi) ----------
certifi==2026.7.22
charset-normalizer==3.4.9
greenlet==3.5.5
idna==3.18
pyee==13.0.1
soupsieve==2.9.2
typing_extensions==4.16.0
urllib3==2.7.0

# --- Opsional -------------------------------------------------------------
# google_news_scraper.py mengimpor `newspaper` di dalam blok try/except dan
# jatuh ke ekstraksi BeautifulSoup bila paket tidak tersedia. Paket newspaper3k
# tidak dicantumkan karena sudah tidak dirawat dan rantai dependensinya
# (nltk, Pillow, tldextract) berat untuk keuntungan yang kecil. Jalur bs4
# adalah jalur yang dipakai di produksi.
